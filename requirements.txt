# ============================================================
# API Dependencies — Store Intelligence API
# ============================================================
# FastAPI: modern async web framework for building REST APIs
fastapi==0.115.12
# Uvicorn: ASGI server that runs FastAPI applications
uvicorn[standard]==0.34.3
# Pydantic: data validation using Python type hints
pydantic==2.11.3
# aiosqlite: async SQLite driver for non-blocking DB operations
aiosqlite==0.21.0
# structlog: structured JSON logging for production observability
structlog==25.4.0
# python-multipart: required for FastAPI form data parsing
python-multipart==0.0.20
# httpx: async HTTP client (used for testing + event forwarding)
httpx==0.28.1
# websockets: WebSocket support for live dashboard
websockets==15.0.1

# ============================================================
# Testing Dependencies
# ============================================================
pytest==8.3.5
pytest-asyncio==0.26.0
pytest-cov==6.1.1
